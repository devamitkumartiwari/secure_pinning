name: secure_pinning_http_example
description: >
  Minimal usage example for secure_pinning_http.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  secure_pinning: ^1.0.0
  secure_pinning_http:
    path: ../

dependency_overrides:
  secure_pinning:
    path: ../../../
  secure_pinning_platform_interface:
    path: ../../secure_pinning_platform_interface
